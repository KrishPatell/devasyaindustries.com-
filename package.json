{
  "name": "devasya-web",
  "version": "1.2.0",
  "private": true,
  "description": "Static export of the Devasya Industries website (Webflow).",
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "check": "node scripts/health-check.mjs",
    "check:live": "node scripts/health-check.mjs --live",
    "normalize": "node scripts/normalize-shared.mjs && node scripts/apply-client-changes.mjs",
    "sync": "node scripts/sync-site.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
